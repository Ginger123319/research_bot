# guofan 本地环境配置（可选）
# 使用方法：cp .env.local.example .env.local，取消注释并填写路径
#
# 此文件已被 .gitignore 忽略，不会提交到 git。
# setup.sh 会在检测到共享目录时自动写入 SHARED_RESULTS_DIR，无需手动操作。

# ── Dashboard 共享历史结果 ────────────────────────────────────────────────────
# serve.py 会同时读取本地 results/ 和此路径下的 INDEX.yaml，
# 在 Dashboard 中合并展示（共享条目带 [共享] 徽章，只读参考）。
# setup.sh 会在检测到共享 NFS 时自动填写此项。
# SHARED_RESULTS_DIR=/mnt/ai-infra/users/shared/benchmark-results/guofan/results

# ── VictoriaMetrics 监控端点 ──────────────────────────────────────────────────
# 默认值：http://172.21.52.62:8481/select/0/prometheus
# 如果你的环境中 VM 地址不同，取消注释并修改：
# VM_ENDPOINT=http://<your-vm-host>:8481/select/0/prometheus
