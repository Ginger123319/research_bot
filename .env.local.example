# guofan 本地环境配置（可选）
# 使用方法：cp .env.local.example .env.local，取消注释并填写路径
#
# 此文件已被 .gitignore 忽略，不会提交到 git。

# results/ 目录路径
# 默认：克隆目录下的本地 results/（$PROJECT_DIR/results/）
# 如需指向共享目录（可查阅他人历史分析结果），取消注释：
# RESULTS_DIR=/mnt/ai-infra/users/shared/benchmark-results/guofan/results
